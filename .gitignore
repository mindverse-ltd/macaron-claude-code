# Deps
node_modules/
.pnp
.pnp.js

# Build output — NOT tracked (committing hash-named Vite bundles made every
# PR conflict on rebuild). The npx/pkg.pr.new tarball still ships prebuilt
# dist: `prepack` builds it fresh into the workspace and package.json `files[]`
# packs it, independent of git. The marketplace git-clone flow builds on the
# first `start.sh` run (~60s source-build fallback).
dist/
build/
*.tsbuildinfo

# Env / secrets
.env
.env.*
!.env.example

# Editor / OS
.DS_Store
.vscode/
.idea/
*.swp
*~

# Logs & tmp
*.log
/tmp/
.cache/

# Claude Code local
.claude/settings.local.json
