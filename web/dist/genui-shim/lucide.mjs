// lucide-react shim — icons are plain components without React hook state, so
// it's safe to use esm.sh build directly (no React instance sharing required).
// If user has esm.sh blocked, swap to a vendored bundle (see lucide-gen script).
export * from 'https://esm.sh/lucide-react@0.577?dev';
